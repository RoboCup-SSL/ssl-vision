#all files below belong to the vartype system:

PREFIX = $${SHARED_DIR}/gl

INCLUDEPATH += $${PREFIX}

SOURCES += $${PREFIX}/glcamera.cpp
HEADERS += $${PREFIX}/glcamera.h

SOURCES += $${PREFIX}/globject.cpp
HEADERS += $${PREFIX}/globject.h

